# Frontend environment variables (Vite)

# Base URL for API calls (keep empty to use same-origin)
VITE_API_BASE_URL=

# App metadata
VITE_APP_NAME=PMS Portal
VITE_APP_VERSION=0.0.0

# Toggle dev-only features inside the frontend
VITE_DEV_MODE=false

