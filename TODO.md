# TODO

- [ ] Implement ONLY Vite watcher fix in `vite.config.ts` to ignore `pms_store.json` (and uploads/logs) during runtime.
- [ ] Run `npm run build`.
- [ ] Report only the changed lines from `vite.config.ts`.

