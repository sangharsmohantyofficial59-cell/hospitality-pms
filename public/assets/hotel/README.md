Hotel Media Management System

This folder hosts all hotel media assets referenced by `src/config/hotel/media.ts`.

Place production assets into subfolders below.
Code will hide media sections when arrays are empty.

